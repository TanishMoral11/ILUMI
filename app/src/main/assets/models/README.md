# 3D Models Directory

This directory is intended for `.glb` 3D models used in the AR Jewelry Try-On System.

**Required action:**
Please place your `necklace.glb` file in this directory (`app/src/main/assets/models/necklace.glb`) before running the AR MVP, as the application relies on it to load the model.
